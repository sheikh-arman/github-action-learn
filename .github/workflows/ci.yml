name: "Workflowample"
on:
  schedule:
  - cron: '*/6 * * * *'

jobs:
  hello:
    name: "Say hello"
    runs-on: ubuntu-latest
    steps:
    - name: Display message
      run: echo "Hello from GitHub Actions!"
